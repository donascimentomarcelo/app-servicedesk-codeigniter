angular.module("hardware").value("config",{
    
    baseUrl : "http://localhost"
    
});

angular.module("inventarioConfig").value("config",{
    
    baseUrl : "http://localhost"
    
});

angular.module("software").value("config",{
    
    baseUrl : "http://localhost"
    
});